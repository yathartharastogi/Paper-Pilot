{
  "name": "paperpilot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check app.js && node --check server.js"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
